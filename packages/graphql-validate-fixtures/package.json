{
  "name": "graphql-validate-fixtures",
  "description": "Validates JSON fixtures for GraphQL responses against the associated operations and schema",
  "version": "0.7.0",
  "main": "lib/index.js",
  "license": "MIT",
  "bin": "./bin/graphql-validate-fixtures",
  "repository": "https://github.com/lemonmade/graphql-tools/tree/master/packages/graphql-validate-fixtures",
  "tags": [
    "graphql"
  ],
  "scripts": {
    "pretest": "yarn run build",
    "build": "tsc",
    "prepublish": "in-publish && yarn run build || :"
  },
  "dependencies": {
    "chalk": "^1.1.3",
    "fs-extra": "^3.0.1",
    "glob": "^7.1.2",
    "graphql": "^0.10.1",
    "graphql-tool-utilities": "^0.6.2",
    "yargs": "^8.0.1"
  },
  "devDependencies": {
    "in-publish": "^2.0.0",
    "typescript": "^2.3.4"
  }
}
