import { Workspace } from '../../workspace';
import Runner from '../../runner';
export declare const prettierIgnorePaths: string[];
export declare function init(workspace: Workspace, runner: Runner): void;
