name: quilt
type: node
up:
  - node:
      version: v12.14.0
      yarn: 1.22.5
commands:
  __default__: start
  build: yarnpkg build
  check: yarnpkg check
  lint: yarnpkg lint
  start: echo "Running quilt is not really a concept that makes sense."
  test: yarnpkg test
  generate: yarnpkg generate
