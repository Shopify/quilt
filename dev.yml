name: quilt
type: node
up:
  - node:
      version: v16.13.0
      yarn: v1.22.15
commands:
  __default__: start
  build: yarnpkg build
  check: yarnpkg check
  lint: yarnpkg lint
  start: echo "Running quilt is not really a concept that makes sense."
  test: yarnpkg test
  generate: yarnpkg generate
