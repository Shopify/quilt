{
  "name": "graphql-tools",
  "private": true,
  "license": "MIT",
  "scripts": {
    "ts": "tsc --noEmit",
    "pretest": "rm -rf packages/graphql-tool-utilities/node_modules/graphql && rm -rf packages/graphql-validate-fixtures/node_modules/graphql && node packages/graphql-validate-fixtures/scripts/introspection.js",
    "test": "jest --config ./config/jest/config.json",
    "test:watch": "yarn run test -- --watch",
    "check": "yarn run ts && yarn run test"
  },
  "devDependencies": {
    "@types/jest": "^19.2.3",
    "@types/node": "^7.0.18",
    "graphql": "^0.9.6",
    "jest": "^20.0.1",
    "lerna": "^2.0.0-rc.4",
    "ts-jest": "^20.0.3",
    "typescript": "^2.3.2"
  },
  "dependencies": {}
}
