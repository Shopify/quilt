{
  "name": "graphql-tools",
  "license": "MIT",
  "scripts": {
    "pretest": "rm -rf packages/graphql-tool-utilities/node_modules/graphql && rm -rf packages/graphql-validate-fixtures/node_modules/graphql",
    "test": "jest --config ./config/jest/config.json",
    "test:watch": "yarn run test -- --watch"
  },
  "devDependencies": {
    "@types/jest": "^19.2.3",
    "graphql": "^0.9.6",
    "jest": "^20.0.1",
    "lerna": "^2.0.0-rc.4",
    "ts-jest": "^20.0.3",
    "typescript": "^2.3.2"
  },
  "dependencies": {
    "@types/glob": "^5.0.30",
    "chalk": "^1.1.3",
    "yargs": "^8.0.1"
  }
}
